{
  "name": "fliplens",
  "version": "1.0.0",
  "description": "A web application that helps thrift resellers evaluate resale potential of clothing items using eBay market data",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && python app.py",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["thrift", "resale", "ebay", "market-data", "profit-calculator"],
  "author": "Arezzio Rietti",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
